import styled from "styled-components";

export const StyledWrapper = styled.div`
  padding: 16px;
  .heading__title {
    display: flex;
    align-items: center;
  }
  svg {
    margin-left: 8px;
    &:hover {
      cursor: pointer;
      color: #00a8ff;
    }
  }
`;
